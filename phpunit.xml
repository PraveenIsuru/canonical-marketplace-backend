<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!--
            Tests run against a real PostgreSQL database, not SQLite in memory.
            Two reasons: PostGIS has no SQLite equivalent, and the seller list query
            computes distance in the database, so a test on SQLite would not exercise
            the thing most likely to break. canonical_marketplace_test is separate
            from the development database and is rebuilt by RefreshDatabase.
        -->
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_DATABASE" value="canonical_marketplace_test"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <!--
            EP-51 is off unless a test turns it on.

            The queue runs synchronously here, so a dispatched revalidation would make a
            real HTTP request to a frontend that is not running, on every test that
            creates a version. The tests that care about the webhook enable it and fake
            the HTTP client, which is also the only way to assert what was sent.
        -->
        <env name="REVALIDATE_ENABLED" value="false"/>
        <!--
            Catalogue caching is left ON, deliberately.

            The array store is per test, so it exercises the invalidation without
            leaking between tests. A suite that disabled it would pass happily while the
            cache served yesterday's price to every real visitor, which is precisely the
            failure worth catching here.
        -->
        <!--
            Scout is disabled outright in tests.

            Product is Searchable, so without this every factory created product is
            pushed to the real Meilisearch index and every rollback leaves it there.
            It polluted the live index with test data and displaced the seeded
            catalogue, which made a manual search return nothing while the suite still
            passed. Tests must never write to a shared external service.
        -->
        <env name="SCOUT_DRIVER" value="null"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
